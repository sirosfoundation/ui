---
title: square
---
import { Button } from '@sirosfoundation/ui/react';
import { Plus } from 'lucide-react';

<Button variant="primary" square icon={Plus} aria-label="Add" />
<Button variant="secondary" square icon={Plus} aria-label="Add" />
<Button variant="outline" square icon={Plus} aria-label="Add" />
